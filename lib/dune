(rule
  (mode (promote (until-clean)))
  (deps (sandbox always))
 (targets (dir js))
 (action
   (run  mkdir %{targets})))
 (rule
  (mode (promote (until-clean)))
  (deps (sandbox always))
 (targets (dir es6))
 (action
   (run  mkdir %{targets}))
 )
