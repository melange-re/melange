// Generated by Melange

import * as Caml_js_exceptions from "melange.js/caml_js_exceptions.mjs";

function raise_invalid_argument(message) {
  throw new Caml_js_exceptions.MelangeError("Invalid_argument", {
      MEL_EXN_ID: "Invalid_argument",
      _1: message
    });
}

const caml_bigstring_get16_raw = (function(ba, i, invalid) {
    if (i < 0 || i + 1 >= ba.dims[0])
      return invalid("index out of bounds");
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    return dv.getUint16(i, true);
  }
);

function caml_bigstring_get16(ba, i) {
  return caml_bigstring_get16_raw(ba, i, raise_invalid_argument);
}

const caml_bigstring_get16u = (function(ba, i) {
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    return dv.getUint16(i, true);
  }
);

const caml_bigstring_set16_raw = (function(ba, i, v, invalid) {
    if (i < 0 || i + 1 >= ba.dims[0])
      return invalid("index out of bounds");
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    dv.setInt16(i, v, true);
  }
);

function caml_bigstring_set16(ba, i, v) {
  caml_bigstring_set16_raw(ba, i, v, raise_invalid_argument);
}

const caml_bigstring_set16u = (function(ba, i, v) {
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    dv.setInt16(i, v, true);
  }
);

const caml_bigstring_get32_raw = (function(ba, i, invalid) {
    if (i < 0 || i + 3 >= ba.dims[0])
      return invalid("index out of bounds");
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    return dv.getInt32(i, true);
  }
);

function caml_bigstring_get32(ba, i) {
  return caml_bigstring_get32_raw(ba, i, raise_invalid_argument);
}

const caml_bigstring_get32u = (function(ba, i) {
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    return dv.getInt32(i, true);
  }
);

const caml_bigstring_set32_raw = (function(ba, i, v, invalid) {
    if (i < 0 || i + 3 >= ba.dims[0])
      return invalid("index out of bounds");
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    dv.setInt32(i, v, true);
  }
);

function caml_bigstring_set32(ba, i, v) {
  caml_bigstring_set32_raw(ba, i, v, raise_invalid_argument);
}

const caml_bigstring_set32u = (function(ba, i, v) {
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    dv.setInt32(i, v, true);
  }
);

const caml_bigstring_get64_raw = (function(ba, i, invalid) {
    if (i < 0 || i + 7 >= ba.dims[0])
      return invalid("index out of bounds");
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    var lo = dv.getUint32(i, true);
    var hi = dv.getInt32(i + 4, true);
    return [hi, lo];
  }
);

function caml_bigstring_get64(ba, i) {
  return caml_bigstring_get64_raw(ba, i, raise_invalid_argument);
}

const caml_bigstring_get64u = (function(ba, i) {
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    var lo = dv.getUint32(i, true);
    var hi = dv.getInt32(i + 4, true);
    return [hi, lo];
  }
);

const caml_bigstring_set64_raw = (function(ba, i, v, invalid) {
    if (i < 0 || i + 7 >= ba.dims[0])
      return invalid("index out of bounds");
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    dv.setUint32(i, v[1], true);
    dv.setInt32(i + 4, v[0], true);
  }
);

function caml_bigstring_set64(ba, i, v) {
  caml_bigstring_set64_raw(ba, i, v, raise_invalid_argument);
}

const caml_bigstring_set64u = (function(ba, i, v) {
    var dv = new DataView(ba.data.buffer, ba.data.byteOffset, ba.data.byteLength);
    dv.setUint32(i, v[1], true);
    dv.setInt32(i + 4, v[0], true);
  }
);

const caml_bigstring_blit_ba_to_bytes_raw = (function(ba, ba_off, bytes, bytes_off, len, invalid) {
    if (ba.kind !== 12)
      return invalid("caml_bigstring_blit_ba_to_bytes: kind mismatch");
    if (len < 0 || ba_off < 0 || ba_off > ba.data.length - len ||
        bytes_off < 0 || bytes_off > bytes.length - len)
      return invalid("index out of bounds");
    for (var i = 0; i < len; i++) {
      bytes[bytes_off + i] = ba.data[ba_off + i];
    }
  }
);

function caml_bigstring_blit_ba_to_bytes(ba, ba_off, bytes, bytes_off, len) {
  caml_bigstring_blit_ba_to_bytes_raw(ba, ba_off, bytes, bytes_off, len, raise_invalid_argument);
}

const caml_bigstring_blit_bytes_to_ba_raw = (function(bytes, bytes_off, ba, ba_off, len, invalid) {
    if (ba.kind !== 12)
      return invalid("caml_bigstring_blit_bytes_to_ba: kind mismatch");
    if (len < 0 || bytes_off < 0 || bytes_off > bytes.length - len ||
        ba_off < 0 || ba_off > ba.data.length - len)
      return invalid("index out of bounds");
    for (var i = 0; i < len; i++) {
      ba.data[ba_off + i] = bytes[bytes_off + i];
    }
  }
);

function caml_bigstring_blit_bytes_to_ba(bytes, bytes_off, ba, ba_off, len) {
  caml_bigstring_blit_bytes_to_ba_raw(bytes, bytes_off, ba, ba_off, len, raise_invalid_argument);
}

const caml_bigstring_blit_ba_to_ba_raw = (function(src, src_off, dst, dst_off, len, invalid) {
    if (src.kind !== 12 || dst.kind !== 12)
      return invalid("caml_bigstring_blit_ba_to_ba: kind mismatch");
    if (len < 0 || src_off < 0 || src_off > src.data.length - len ||
        dst_off < 0 || dst_off > dst.data.length - len)
      return invalid("index out of bounds");
    dst.data.set(src.data.subarray(src_off, src_off + len), dst_off);
  }
);

function caml_bigstring_blit_ba_to_ba(src, src_off, dst, dst_off, len) {
  caml_bigstring_blit_ba_to_ba_raw(src, src_off, dst, dst_off, len, raise_invalid_argument);
}

export {
  caml_bigstring_get16,
  caml_bigstring_get16u,
  caml_bigstring_set16,
  caml_bigstring_set16u,
  caml_bigstring_get32,
  caml_bigstring_get32u,
  caml_bigstring_set32,
  caml_bigstring_set32u,
  caml_bigstring_get64,
  caml_bigstring_get64u,
  caml_bigstring_set64,
  caml_bigstring_set64u,
  caml_bigstring_blit_ba_to_bytes,
  caml_bigstring_blit_bytes_to_ba,
  caml_bigstring_blit_ba_to_ba,
}
/* No side effect */
