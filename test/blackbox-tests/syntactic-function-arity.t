
  $ . ./setup.sh
  $ cat > x.ml <<EOF
  > let sum = function a -> function b -> a + b
  > let sum2 = fun a -> fun b -> a + b
  > let sum3 = fun (a, b) -> fun c -> a + b + c
  > EOF

  $ melc -drawlambda x.ml
  (setglobal X!
    (let
      (sum/267 =
         (function a/269[int] (function b/270[int] : int (+ a/269 b/270)))
       sum2/271 =
         (function a/273[int] (function b/274[int] : int (+ a/273 b/274)))
       sum3/275 =
         (function param/280
           (let
             (b/278 =a (field_imm :[]/1 param/280)
              a/277 =a (field_imm :[]/0 param/280))
             (function c/279[int] : int (+ (+ a/277 b/278) c/279)))))
      (makeblock 0module/exports sum/267 sum2/271 sum3/275)))
  // Generated by Melange
  'use strict';
  
  
  function sum(a, b) {
    return a + b | 0;
  }
  
  function sum2(a, b) {
    return a + b | 0;
  }
  
  function sum3(param) {
    const b = param[1];
    const a = param[0];
    return function (c) {
      return (a + b | 0) + c | 0;
    };
  }
  
  module.exports = {
    sum,
    sum2,
    sum3,
  }
  /* No side effect */
