(dirs :standard \ docs ocaml node_modules native ninja nix site vendor
  lib/4.06.1)

(env
 (dev
  (env-vars
   (CPPO_FLAGS -U=BS_RELEASE_BUILD)))
 (release
  (env-vars
   (CPPO_FLAGS -D=BS_RELEASE_BUILD))
  (ocamlopt_flags
   (:standard -O3 -unbox-closures))))

(data_only_dirs lib syntax)

